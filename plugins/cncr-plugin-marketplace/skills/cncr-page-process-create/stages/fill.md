# 阶段4：区域结果回填与全局状态实现

## 目标
- 根据业务 MD 第3节的区域定义完成区域结果回填，并落实第4节规定的全局 `loading / empty / error` 状态处理。

## 必须输出
- 区域查询结果回填结论
- 全局 loading 状态实现结论
- 全局 empty 状态实现结论
- 全局 error 状态实现结论
- 成功OK / 失败FAIL 结论

## 设计预览约束（强制）
- 若前置阶段已生成并确认 pencli 设计预览，则本阶段实现必须以该设计预览为直接参考。
- 不得擅自改动已确认的布局结构、组件层级、区块顺序和主要视觉方向。
- 若业务 MD 与 pencli 设计预览存在冲突，默认以用户确认后的设计预览为页面结构参考，同时仍需遵守业务 MD 的数据契约与状态约束。
- 若确需偏离设计预览，必须先明确说明偏离原因，不得直接输出“实现完成”。

## 强约束
- 功能 Skill 不定义固定业务范式。
- 区域数量、查询来源、字段需求全部以业务 MD 第3节为准。
- 全局状态处理以业务 MD 第4节为准。

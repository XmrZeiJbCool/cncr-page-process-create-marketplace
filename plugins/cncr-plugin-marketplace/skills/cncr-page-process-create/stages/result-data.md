# 阶段3：统一数据契约与 resultData

## 目标
- 将接口响应归一化到统一结果对象，再供 UI 回填使用。

## 必须输出
- 归一化入口文件
- `resultData` 关键字段
- 关键 helper / adapter 名称
- 关键字段转换说明（如数值转换、展示字段优先级）

## 禁止事项
- 禁止 UI 直接读取原始响应字段。
- 禁止把具体业务字段范式写死到功能 Skill。
